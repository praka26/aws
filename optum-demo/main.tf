/* module "s3" {
  source = "terraform-aws-modules/s3-bucket/aws"
  #bucket name should be unique
  bucket = "optum-demo"
  acl    = "private"

  versioning = {
    enabled = true
  }
} */
/* module "s3" {
    source = "./modules/s3"
    #bucket name should be unique
    bucket_name = "optum2"       
} */
